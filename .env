# Port (default: 3004 | Cloud Run inject 8080)
PORT=3003

# JWT secret — ต้องตรงกับ tokenKey ใน zyra-api
tokenKey=Akso3foAgoGaAosT@fher

# Allowed WebSocket origins (comma-separated)
# local dev ใช้ * ได้ | production ใส่ domain จริง
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com